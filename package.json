{
  "name": "sanlee-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview --port 4322",
    "qa": "npm run build && npm run gates",
    "gates": "node scripts/link-check.cjs && node scripts/check-published-metrics.cjs && node scripts/private-repo-check.cjs && node scripts/mobile-qa.cjs"
  },
  "dependencies": {
    "astro": "^7.1.0"
  }
}
