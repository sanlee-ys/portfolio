# This is a no-build static site — no package.json/lockfile at the root.
# scripts/package.json pins Playwright for the QA tooling only; it's tracked,
# node_modules/ under it is not.
node_modules/

graphify-out/
