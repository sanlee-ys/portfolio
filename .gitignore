# Since ADR-006 the site IS built: Astro at the root (package.json +
# package-lock.json are tracked, and CI runs `npm ci`), output to dist/.
# scripts/package.json separately pins Playwright for the QA tooling; both
# lockfiles are tracked, neither node_modules/ is.
node_modules/
dist/
.astro/

graphify-out/

# Machine-local private-repo names for the Layer C pre-commit guard.
scripts/.private-repos.local
